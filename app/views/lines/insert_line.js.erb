var first = '<%= @first.to_i %>'
var first_val = '<%= @first %>'
var new_order_number = '<%= @new_order_number %>'
var line_entity = '<%= escape_javascript( render partial: "shared/line", locals: { line: @lyric_line } ) %>'


$('#entity-' + first).after(line_entity)
$('#form-' + first + " .second").attr('value', new_order_number);
$('#form-hidden-' + first).after('<%= escape_javascript( hidden_field_tag "lyric_lines[]", "", id: "form-hidden-#{@lyric_line.order_number.to_i}" )%>')

