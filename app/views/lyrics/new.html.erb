
<p>Warning, Unless you HATE the existing lyrics, you should use the editing features instead of creating a new Lyric. This form is for COMPLETE overhauls only.</p>

<%= form_for @lyric do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<%= f.label :text, 'Enter new Lyrics:' %>
	<%= text_area_tag 'text'  %>
	<%= f.hidden_field :topic_id, value: @topic_id %>
	<%= f.submit %>
<% end %>