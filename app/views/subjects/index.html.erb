<div class="offset-by-one six columns">
<%= render @subjects %>
</div>
<% if user_signed_in? %>
<div class="six columns">
	<h3>New subject:</h3>

	<%= form_for @subject do |f| %>
		<%= f.label :title %>
		<%= f.text_field :title %>
		<%= f.submit %>
	<% end %>
</div>
<% end %>