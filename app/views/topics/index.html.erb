<h2>Topics for <%= @subject.title %></h2>

<%= render @topics %>

<h3>New topic:</h3>
<%= form_for @topic do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<%= f.hidden_field :subject_id, value: @subject.id %>
	<%= f.submit %>
<% end %>